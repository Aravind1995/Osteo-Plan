OsteoPlan patched export bundle

This ZIP contains:
- index.html
- styles.css
- app.js  <-- patched: includes Retina/iPhone Safari PNG export fix
- icon.png

Usage:
Open index.html on a secure host (HTTPS recommended). On iPhone, use the 'Export PNG' button.
The patch fixes retina scaling and Safari blob quirks so exported PNGs match desktop exports.

If you want me to instead replace only a specific export handler or produce a smaller patch file, tell me.
